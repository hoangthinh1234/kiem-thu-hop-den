# Prompt dùng với Codex trong VS Code

Bạn là trợ lý lập trình cho bài **Bài thực hành 03 – Kiểm thử hộp đen** của môn **Đánh giá và kiểm định chất lượng phần mềm**.

Hãy xây dựng một project Python 3 hoàn chỉnh theo đúng yêu cầu sau:

1. Có 8 bài:
   - Tính chu vi hình chữ nhật.
   - Tính diện tích hình chữ nhật.
   - Giải phương trình bậc hai.
   - Tính số ngày của một tháng.
   - Kiểm tra n có phải số nguyên tố.
   - Tính S = 1 - 2 + 3 - 4 + ... + n.
   - Tìm UCLN của a và b.
   - Tính S = 1! + 2! + ... + n!, bắt buộc có hàm factorial.
2. Tổ chức project rõ ràng: `src/`, `tests/`.
3. Viết chương trình có kiểm tra đầu vào và xử lý dữ liệu không hợp lệ bằng exception.
4. Viết test theo black-box, không dựa vào chi tiết cài đặt bên trong.
5. Dùng hai kỹ thuật: Equivalence Partitioning và Boundary Value Analysis.
6. Mỗi bài có dữ liệu hợp lệ và tối thiểu một dữ liệu không hợp lệ hoặc giá trị biên.
7. Dùng pytest.
8. Tạo `README.md` giải thích cách chạy, cấu trúc project, cách áp dụng kiểm thử hộp đen cho từng bài.
9. Tạo `TEST_CASES.md` liệt kê ID, kỹ thuật, input, expected result.
10. Sau khi tạo xong, chạy toàn bộ test và sửa cho đến khi tất cả test PASS.
11. Không thay đổi phạm vi 8 bài nếu không cần thiết.
12. Khi hoàn thành, in ra cây thư mục và câu lệnh chạy test.

Sau đó hãy kiểm tra lại:
- lỗi cú pháp;
- import;
- trường hợp biên;
- dữ liệu không hợp lệ;
- kết quả expected.

Cuối cùng báo cáo ngắn: đã tạo/sửa file nào, tổng số test, bao nhiêu PASS/FAIL.
